[project]
name = "thoughtful-ai-agent"
version = "0.1.0"
description = "Simple customer support agent for Thoughtful AI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "typer>=0.12.3",
]

[project.scripts]
thoughtful-ai-agent = "thoughtful_ai_agent.cli:main"
